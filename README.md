# Webhook Automation Engine

Simple Node.js webhook system with rule-based event processing.

## Features
- Express server
- Webhook endpoint (Monday simulation)
- JWT authentication middleware
- Rule engine for event processing
- Action system (create ticket / log event)

## Run locally

```bash
npm install
npm start